import java.util.Arrays;

public class SortArray<TYPE> {
    public static <TYPE> TYPE[] sortArray(TYPE[] names) {
        Arrays.sort(names);
        return names;
    }

    public static long[] sortArray(long[] names) {
        Arrays.sort(names);
        return names;
    }

    public static int[] sortArray(int[] names) {
        Arrays.sort(names);
        return names;
    }

    public static short[] sortArray(short[] names) {
        Arrays.sort(names);
        return names;
    }

    public static byte[] sortArray(byte[] names) {
        Arrays.sort(names);
        return names;
    }

    public static double[] sortArray(double[] names) {
        Arrays.sort(names);
        return names;
    }

    public static float[] sortArray(float[] names) {
        Arrays.sort(names);
        return names;
    }
  }




// DESCRIPTION:
// Just a simple sorting usage. Create a function that returns the elements of the input-array / list sorted in lexicographical order.

// SORTING ARRAYS FUNDAMENTALS
